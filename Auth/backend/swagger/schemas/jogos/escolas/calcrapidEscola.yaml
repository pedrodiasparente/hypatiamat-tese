type: array
items:
  type: object
  properties:
    codprofessor:
      type: string
      description: 'Código do professor.'
    nome:
      type: string
      description: 'Nome do professor.'
    pontcerta:
      type: integer
      description: 'Pontuação total acertada pelas turmas do professor.'
    ponterrada:
      type: integer
      description: 'Pontuação total errada pelas turmas do professor.'
    oprealizadas:
      type: integer
      description: 'Operações realizadas pelas turmas do professor.'
    frequencia:
      type: integer
      description: 'Frequência das turmas do professor.'